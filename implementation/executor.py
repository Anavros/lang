

def run(operations):
    for op in operations:
        print(op)

        #f, args = op
        #try:
            #scope[f](args)
        #except KeyError:
            #print("Unknown identifier: '{}'.".format(f))
#
#
#def assign(args):
    #scope[args[0]] = args[1]
#
#
#scope = {
    #'print': print,
    #'assign': assign,
#}
