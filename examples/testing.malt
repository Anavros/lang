
import time

def sieve(int ceiling) => bool array {
    var bool array primes = ???
    var int i = 2
    loop {
        break if i >= ceiling+1
        if primes[i] == true {
            let int j = i*2
            loop {
                break if j >= ceiling+1
                primes[j] = false
                j += 1
            }
        }
        i += 1
    }
    return primes
}

END OF FILE

Conclusions:
- A while loop would still be a good idea.
- Inline if statements are cool.
- Incrementing a counter is a pretty common
  looping task.
- Type specification is going to be a doozy.
