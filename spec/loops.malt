

# Infinite Loop
loop { f(); }
loop {
    f();
    break if not g();
}

# Conditional Loop
while i < 10 {
    f();
}
until i >= 10 {
    f();
}

# Looping `n` Times
loop * 10 {
}

# Inline Loops
print(x) for x in xs;
Vector(x for x in xs);

# Repeat Expansion
if " " in word or "/" in word or ":" in word { }
if |" ", "/", ":"| in word { }

x = 0; y = 0; z = 0;
|x,y,z| = 0;
