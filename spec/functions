

# Normal functions.
name = Func (a:A, b:B) : C {
    statements;
}

# Entry points can be specified by assigning a
# function to sys.entry.
sys.entry = Func {
}

# Anonymous functions aren't really a thing.
Func (a:A, b:B) : C {
}
